{
  "name": "nodefetch",
  "version": "1.0.0",
  "description": "A fetch, in NodeJS",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && node dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Randomness",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^12.7.8"
  },
  "dependencies": {
    "@types/getos": "^3.0.0",
    "colors": "^1.4.0",
    "getos": "^3.1.1",
    "systeminformation": "^5.6.4"
  }
}
